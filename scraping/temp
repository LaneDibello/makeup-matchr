from selenium import webdriver
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.common.by import By
from selenium.webdriver.support import expected_conditions as ec
from selenium.webdriver.support.ui import WebDriverWait

wait = WebDriverWait(driver, 5)

# Creates an Options object so the browser can run headlessly
options = Options()

# Sets the headless option to true
options.headless = False

# Disables unnecessary prints
options.add_experimental_option('excludeSwitches', ['enable-logging'])


# This function collects a list of products from the base page
def get_products(driver: webdriver, url: str, product_class: str, next_class: str):

    # Load the site


    # Make sure the site has been loaded
    wait.until(ec.element_to_be_clickable((By.CLASS_NAME, product_class)))